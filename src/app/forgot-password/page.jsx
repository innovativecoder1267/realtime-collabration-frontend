import Forgot from "../components/forgot-password"
import { Suspense } from "react"
export default function ForgotPasswordPage() {
  <Suspense fallback={<div>Loading forgot</div>}>
  return <Forgot/>
  </Suspense>
}
